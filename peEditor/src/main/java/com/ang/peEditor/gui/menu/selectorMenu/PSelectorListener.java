package com.ang.peEditor.gui.menu.selectorMenu;

/**
 * Interface for receiving events from the selector.
 * @see PSelector
 */
public interface PSelectorListener {
	void selectionMade(PSelector selector);
}
